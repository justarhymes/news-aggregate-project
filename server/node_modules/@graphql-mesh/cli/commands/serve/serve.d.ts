import { GraphQLSchema } from 'graphql';
import { Logger } from 'winston';
import 'json-bigint-patch';
export declare function serveMesh(logger: Logger, schema: GraphQLSchema, contextBuilder: (initialContextValue?: any) => Promise<Record<string, any>>, fork?: string | number, port?: string | number, exampleQuery?: string): Promise<void>;
