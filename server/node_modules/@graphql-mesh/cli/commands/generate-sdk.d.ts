import { GraphQLSchema } from 'graphql';
export declare function generateSdk(schema: GraphQLSchema, { operations: operationsPaths, depth: depthLimit, }: {
    operations?: string[];
    depth?: number;
}): Promise<string>;
